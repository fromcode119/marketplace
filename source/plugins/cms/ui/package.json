{
  "name": "@fromcode-plugin/cms-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.ts --bundle --format=esm --outfile=bundle.js --external:react --external:react-dom --external:lucide-react --jsx=automatic",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
